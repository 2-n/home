#!/usr/bin/env sh

fn='Ttyp0:size=12'
nb='#000000'
nf='#FFFFFF'
sb='#FFFFFF'
sf='#000000'

# doing funny geometry because dmenu doesnt have an
# offset opt like lemonbar does to center text better 

dmenu_run -b -h 23  \
-x 60 -y -3 -z 2440 \
-nb "$nb" -nf "$nf" \
-sb "$sb" -sf "$sf" \
-fn "$fn"
